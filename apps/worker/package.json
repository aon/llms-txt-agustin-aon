{
  "name": "@llms-txt/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "enqueue": "pnpm -w exec turbo run build --filter=@llms-txt/core && node scripts/enqueue.ts",
    "status": "pnpm -w exec turbo run build --filter=@llms-txt/core && node scripts/status.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1131.0",
    "@aws-sdk/client-s3": "3.1131.0",
    "@aws-sdk/client-secrets-manager": "3.1131.0",
    "@aws-sdk/client-sqs": "3.1131.0",
    "@llms-txt/core": "workspace:*",
    "@llms-txt/crawler": "workspace:*",
    "@llms-txt/llms-txt": "workspace:*"
  },
  "devDependencies": {
    "@aws-sdk/client-cloudformation": "3.1131.0",
    "@types/aws-lambda": "8.10.163",
    "@types/node": "24.13.4",
    "typescript": "7.0.2",
    "vitest": "5.0.0"
  }
}
